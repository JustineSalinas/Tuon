/**
 * Brand names used in UI copy.
 *
 * The creature has a name because an unnamed mascot stays decoration. A named
 * one can carry a sentence — "Tala says you have 12 cards waiting" does work
 * that a picture cannot, and it is what makes an empty state feel like
 * somebody is keeping your place rather than a screen being blank.
 *
 * "Tala" is Filipino for star: short, warm, already a common given name, and
 * it sits naturally beside a mark that means a point of focus. Change it here
 * and every mention follows.
 */
export const CREATURE_NAME = "Tala";

/** Used where the creature is announced to a screen reader. */
export const CREATURE_ROLE = `${CREATURE_NAME}, your study companion`;
